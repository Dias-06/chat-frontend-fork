# shared - сюда кладём то, что используется ВЕЗДЕ и ВСЕГДА

├─ ui/ <- обычные кнопки, инпуты, карточки, спиннеры
│ ├── button/
│ │ ├── Button.tsx <- обычная кнопка
│ │ ├── Button.module.css
│ │ └── index.ts <- `export { Button } from './Button'`
│ ├── input/
│ ├── modal/
│ └── card/
│
├─ lib/ <- чистые функции и хуки
│ └── format-price.ts <- 1000 -> "1 000 ₽"
│
├─ config/ <- то, что одно на весь проект
│ ├── env.ts
│ └── constants.ts
│
└─ assets/ <- картинки и иконки, которые используются везде
├── icons/
└── images/

ЧТО СЮДА НЕЛЬЗЯ:

- Логика пользователя, товара, заказа
- Формы входа, корзины
- Компоненты Header, Footer, Sidebar
